spring:
  application:
    name: hf-community-hub

  datasource:
    url: jdbc:postgresql://mainline.proxy.rlwy.net:45251/railway?sslmode=require
    username: postgres
    password: vzUwvqMmSNiIAbNPQtvNaEauaqOLsEby
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
      connection-test-query: SELECT 1
      connection-init-sql: SELECT 1
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        transaction:
          jta:
            platform: null

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE